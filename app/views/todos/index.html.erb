<p style="color: green"><%= notice %></p>

<% content_for :title, "Todos" %>

<h1>Todos</h1>

<%= form_with url: todos_path, method: :get do |form| %>
  <%= form.label :category, "Filter by category" %>
  <%= form.select :category, category_filter_options(@category) %>
  <%= form.submit "Filter" %>
<% end %>

<div id="todos">
  <% if @todos.any? %>
    <% @todos.each do |todo| %>
      <%= render todo %>
      <p>
        <%= link_to "Show this todo", todo %>
      </p>
    <% end %>
  <% else %>
    <p>No todos found</p>
  <% end %>
</div>

<%= link_to "New todo", new_todo_path %>
